# Python
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
.pytest_cache/

# Rust build artifacts
target/

# Maturin temporary wheels (these are rebuilt on demand)
*.whl

# IDE
.vscode/
.idea/

# NOTE: Cargo.lock is intentionally committed for reproducibility.
# When users run `maturin develop`, they get the exact tested versions
# of rustfft, rayon, and pyo3. Do NOT add Cargo.lock here.
